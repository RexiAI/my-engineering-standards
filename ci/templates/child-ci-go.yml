# Generated CI for Go project
# Template: ci/templates/child-ci-go.yml

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  ci:
    uses: RexiAI/my-engineering-standards/.github/workflows/ci-go.yml@21046d96796467f8238d6d613ce3a552bf3fced0 # pinned; bump deliberately, never track a branch
    with:
      go-version: "1.26"
      deploy-on-main: true
      docker-registry: DOCKER_REGISTRY
    secrets:
      GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
