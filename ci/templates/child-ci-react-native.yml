# Generated CI for React Native project
# Template: ci/templates/child-ci-react-native.yml

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  ci:
    uses: RexiAI/my-engineering-standards/.github/workflows/ci-react-native.yml@21046d96796467f8238d6d613ce3a552bf3fced0 # pinned; bump deliberately, never track a branch
    with:
      node-version: "22"
    secrets:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
