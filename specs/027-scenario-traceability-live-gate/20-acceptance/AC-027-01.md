# AC-027-01: check 2 scoped to in-flight spec numbers

## AC-027-01 — a dangling reference is caught within the current spec; an archived spec's own citation is silently out of scope

Given `scripts/check-scenario-traceability.sh` is run against a tree where
`specs/999-slug/20-acceptance/` defines `AC-999-01` through `AC-999-03`,
and a test file references `AC-999-04` (same in-flight number, no matching
heading) and `AC-700-01` (a different number with no `specs/700-*/` folder
anywhere in the tree)
When check 2 runs
Then `AC-999-04` is reported as a dangling reference (its number, 999, is
  in flight and it has no matching heading), and `AC-700-01` is silently
  skipped — not reported, not counted — because 700 has no current
  `specs/700-*/` folder.

Given the exact tree state of spec 026's own PR branch before archival
(specs/026-ci-security-hardening/ present, plus every other spec's
gate-script self-citations for already-archived spec numbers like 017,
019, 024)
When the fixed `scripts/check-scenario-traceability.sh` runs against that
tree with no arguments
Then it reports 0 violations and exits 0 — down from the unfixed script's
  154 violations against the identical tree.

Checkable by reading code and by running the script:
- `scripts/check-scenario-traceability.sh`'s check-2 block computes
  `IN_FLIGHT_NUMS` from `specs/[0-9][0-9][0-9]-*/` directory names and
  `continue`s past any referenced ID whose number is not in that set,
  before the `SCENARIO_IDS` membership test.
- `bash scripts/check-scenario-traceability.selftest.sh` exits 0, 4 cases
  pass (`pass`, `orphan`, `dangle`, `out_of_flight`).
